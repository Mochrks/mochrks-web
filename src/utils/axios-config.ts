// Re-export from config for backward compatibility
// The actual axios configuration lives in @/config/axios-config
export { default, mediumApiInstance, githubApiInstance } from "@/config/axios-config";
